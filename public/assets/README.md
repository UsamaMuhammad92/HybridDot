# Public assets

Place static assets here:

- `images/` – logos, illustrations, photos
- `icons/` – favicons, app icons
- `fonts/` – custom font files (optional; consider using next/font)

Reference them in code with paths like `/assets/images/logo.png`.
