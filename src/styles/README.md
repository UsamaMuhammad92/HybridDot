# Styles

Use this folder for:

- Global or shared CSS beyond `app/globals.css`
- CSS modules if needed
- Design tokens or theme overrides

Import any file here from `app/layout.tsx` or from the component that needs it.
